baseurl: /singular

# Ignore generated files to avoid watch regeneration loop
exclude: [".sass-cache"]

sass:
  sass_dir: css/_sass

defaults:
  -
    scope:
      # All files in the project
      path: ""
    values:
      layout: "default"
