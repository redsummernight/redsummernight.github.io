language: ruby
rvm:
- 2.1

# Since bundler is used, the `install` step will run `bundle install` by default.
script: jekyll build && htmlproof ./_site --check-html --verbose

env:
  global:
  - NOKOGIRI_USE_SYSTEM_LIBRARIES=true # speeds up installation of html-proofer

cache: bundler
